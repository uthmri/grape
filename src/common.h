#ifndef COMMON_H
#define COMMON_H

int writeStrToLog(const QString&);
bool checkExecutableFileExists(const QString& commandPath);
bool checkFileExists(const QString &commandPath);
bool checkPathExists(const QString &commandPath);


#endif // COMMON_H

